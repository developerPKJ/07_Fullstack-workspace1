spring.application.name=Backend_Project

# contextPath 와 port 번호 지정
server.servlet.context-path=/backend
server.port=8006

# 기존에 JSP 를 쓸 경우에는 메인페이지 URL 주소가,
# 새로이 리액트를 쓸 경우에는 요청 주소의 Entry Point 가
# http://localhost:8006/backend